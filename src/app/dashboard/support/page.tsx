import { SupportClient } from "./support-client";

export const metadata = {
  title: "Support Center",
  description: "Get help from the Team4Job support team",
};

export default function SupportPage() {
  return <SupportClient />;
}
