import { ReferralClient } from "./referral-client";

export const metadata = {
  title: "Referrals & Rewards",
  description: "Earn rewards by inviting users",
};

export default function ReferralsPage() {
  return <ReferralClient />;
}
